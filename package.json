{
  "name": "movvai-agents",
  "version": "0.1.0",
  "description": "Squads autônomas de agentes que se conectam a repositórios — do PO ao delivery.",
  "type": "module",
  "scripts": {
    "setup": "tsx src/setup.ts",
    "squad": "tsx src/orchestrator.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "dotenv": "^17.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
